import { DateMonthPipe } from './date-month.pipe';

describe('DateMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new DateMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
